package org.lfj.lang;

/**
 * A class of applicatives that directly pushes its values onto the stack
 * and then it operates upon them.
 */
public abstract class ExpressionApplicative extends Applicative {}
